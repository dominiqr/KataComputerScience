var stack = ["Manzana","Cebolla","Apio","Naranja","Papaya","Sandía","Melón"];
num = 4

mifuncion(stack,num); {
    for(let i = 0; i < n; i++) {
        
        values.push(stack.pop());
        } 
        return values;
    }
      
    const values = mifuncion(stack,num)
    
    console.log(values);